---
# ------------------------------------------------------------
# Sample role, minimal.
# ------------------------------------------------------------
- name: Create a sample file
  ansible.builtin.copy:
    content: "Hello from Ansible\n"
    dest: /tmp/sample.txt
    mode: "0644"
